---
title: 工作日志与 Bug 标记
description: 工作记录模板 + 已知 bug 标记清单 B-01~B-15（仅标记不修）
type: 按需
---

# 工作日志 / 已知问题标记

> **本文件用途**：记录每一次"读系统 / 改系统 / 排障"的工作，以及把发现的 bug **标记**出来。
> **原则**：本阶段只"读"不"改代码"。bug 仅在此标记（含位置、现象、影响、建议修法），
> 真正修复时新建一条"修复记录"并在对应 bug 行标注 `[已修复-日期]`。
> 模板见末尾。新接手的人先读"已知问题标记清单"，避免重复踩坑。

---

## 一、已知问题标记清单（BUG LOG）

> 状态：`<未处理>` / `<已修复>` / `<待确认>`
> 严重度：`高`（功能不可用）/ `中`（部分功能异常）/ `低`（隐患/整洁度）

| 编号 | 状态 | 严重度 | 位置 | 现象 | 影响 | 建议修法 |
|---|---|---|---|---|---|---|
| B-01 | <未处理> | 中 | `api/JsInteration.java` `updateHopeLightBarColor` vs `js/utils.js` `led.control`→`updateZhyLightBarColor` | 方法名拼写不一致（Hope≠Zhy），H5 调用的名字 App 端不存在 | 灯带颜色控制调用失败 | 统一为 `updateHopeLightBarColor`（或 H5 改 Zhy），二选一 |
| B-02 | <未处理> | 中 | `utils/SwitchReceiver.java`（已实现）/ `AndroidManifest.xml`（未注册） | 物理按键逻辑已写但未在 Manifest 注册 | 按键1/2/3 不生效 | 补 `<receiver>` + 监听 judian 按键广播；或确认废弃后删类 |
| B-03 | <未处理> | 高 | `Constant.WEB` 默认 `index_android.html` vs 前端工程无此文件 | 部署前端后若不改 conf.ini 的 web → App 白屏 | 屏白屏 | 部署工具 type4 必须下发 `web=总控选择.html`；或前端补 `index_android.html` 重定向 |
| B-04 | <未处理> | 高 | H5 `.opendoor` 按钮 → `window.android.opendoor()`（`js/index_10.js`/各页）vs App 无此方法 | 前端"立即开锁"点击无响应 | 开门功能前端不可用 | App 实现 `opendoor()` 走门牌 TCP；或前端改走 `SwitchReceiver` 同路 |
| B-05 | <未处理> | 低 | `AndroidManifest.xml` `application android:exported="true"` | application 级 exported 无效（只对组件有效） | 无功能影响，但误导 | 删除该属性 |
| B-06 | <未处理> | 中 | `res/xml/http_net_config.xml` `cleartextTrafficPermitted=true` + `utils/Constant.java` 会议 host `meeting-test.scasia-core.com:8099`(HTTP) | 全链路明文，含会议后端 | 中间人风险 | 评估改 HTTPS（需同步 security config） |
| B-07 | <未处理> | 低 | `utils/Crc16Util.java` | 无任何调用（死代码） | 包体冗余 | 删除或接入门牌 Modbus |
| B-08 | <未处理> | 中 | `js/utils.js` `host=meeting-test.scasia-core.com:8099` | 测试地址硬编码 | 切生产需改代码 | 抽到 conf.ini / 部署配置 |
| B-09 | <未处理> | 低 | `api/HopeJs.java` 硬编码 `0x00~0x17` 灯带节点 | 灯带数量固定 | 扩灯带需改代码 | 配置化 |
| B-10 | <未处理> | 中 | `亚中.json` `解析网络协议` `查询反馈，调整状态`(bd7e1351) | 注释"待完善"，空调状态回读未闭环 | 空调状态可能不同步 | 完善 querys 反馈解析 |
| B-11 | <未处理> | 低 | `亚中.json` `继电器编号`(8028443f) 仅固化 -1 楼 | 联克继电器其他楼层未配置 | 其他楼层联克继电器不可用 | 补 `links[楼层]` 映射 |
| B-12 | <未处理> | 低 | `亚中.json` crontab 注释"中午屏蔽雷达/晚8过道暖光" | 部分定时逻辑可能未完全实现 | 场景与注释不符 | 核对实现 vs 注释 |
| B-13 | <未处理> | 低 | 各 html 页 `<title>` 均写"5楼区域1" | 模板复制痕迹 | 无功能影响 | 改成真实页名 |
| B-14 | <未处理> | 低 | `MainActivity` 权限被拒 `killProcess` | 无权限直接杀进程，体验差 | 屏黑/重启 | 改为引导授权或降级 |
| B-15 | <未处理> | 中 | `NettyServerHandler` 报文格式 vs `中控上传程序.exe` | 两端协议耦合，改一端需同步另一端 | 联调失败 | 协议版本化 / 文档化帧格式 |

---

## 二、阅读 / 工作记录（按时间倒序，新在最上）

### 2026-08-23 — 文档体系优化（frontmatter + 代码解说默认不加载）
- 背景：用户指出文档需进一步优化——①代码解说通常不加载 ②md 加 YAML frontmatter(文件名+描述)、索引含文件名+描述。参考 Codex/Claude Code 上下文分层方案评估：两条要求合理，采纳。
- 产出：
  - 全部 26 个 md 加 YAML frontmatter（`title`/`description`/`type`），type∈{长驻,按需,分片}
  - 重写 `上下文路由.md`：成为带描述的唯一索引（文件名+描述+类型表 + 场景路由 + 污染防护），明确"代码解说默认不加载，仅显式触发"
  - README 第0节导航改为"文件名+描述+加载策略"，标注代码解说🗄️默认冷存储
- 设计依据：规则层(宪法)长驻；源码注解(代码解说)默认冷存储、按模块显式加载；索引靠 description 做相关性判断省 token；禁止隐式全量读
- 关联：宪法第5B章、上下文路由、各 docs 文件
- 验证：抽看 Constant.md/开发宪法.md frontmatter 注入正确、正文未损

### 2026-08-23 — 上下文治理（路由 + 长驻 + 分片）
- 背景：文档体系强化后，存在上下文爆炸/污染风险；需确保宪法长驻、其余按需、路由可达
- 产出：
  - `docs/上下文路由.md`（地图的地图）：长驻vs按需表、场景→加载路由表、代码解说分片索引、污染防护
  - 宪法开头声明"本文档长驻 + 其余按路由按需加载"
  - README 第0节导航加"加载策略"列 + 路由置顶
- 关键设计：宪法(12.7K)长驻；代码解说已按文件拆分(0.5~2.2K/个)，路由表指向单文件，禁止整目录读；主文档(手册/速查/日志/演化)按场景加载
- 关联：宪法第5B章(文档即代码)、各 docs 文件

### 2026-08-23 — 强化"文档即代码"（代码解说 + 演化日志）
- 背景：用户要求把"文档即代码"升格——①代码即文档（当前源码文档化供理解）②文档即代码（改代码必同步改文档 + 演化日志让 LLM/自己知道发生了什么）
- 产出：
  - `docs/代码解说/`（20 个文件：18 Java + Manifest + 前端3 + NodeRED 节点注解，均为源码白话镜像，未改源码）
  - `docs/代码解说/_索引与规范.md`（编写规范 + 文件清单状态）
  - `docs/演化日志.md`（E-00 初始基线 + 演化纪律 + 改动模板）
  - 宪法新增第 5B 章"文档即代码"（5B.1 代码即文档 / 5B.2 改代码必改文档 / 5B.3 演化日志 / 5B.4 违反后果）
- 关键约束固化：改代码未同步文档 = 改动未完成、禁止上线；LLM 改代码须同时产出文档更新 diff
- 关联文档同步：README 导航、手册开头、宪法 5B 章、本日志
- 注意：本轮仅文档化，零源码改动；E-00 锚定"读懂时系统原貌"

### 2026-08-23 — 制定《开发运维宪法》（docs/开发宪法.md）
- 背景：零经验+无指导者新手，借助 LLM，基于未完工源码完工并长期运维；怕修坏整体、未来对 bug 无判断经验
- 产出：`docs/开发宪法.md`（最高约束，9 章：总则/角色边界/读系统/改系统铁律/模块化/运维脚手架/冒烟checklist/禁止项/演进）
- 核心铁律：现场 bug 才修、潜在 bug 只标；最小改动+解耦三问；硬协议边界不动；PC 工具二进制不改；不可验证/不可回退的改动禁止；改动必留痕三件套+回退预案
- 补充了你未提及但本项目关键的约束：四层部署物版本配套、外部二进制不可改、拒绝 LLM 顺手重构、文档过时=系统损坏、无自动化测试下靠手动 checklist 验证
- 在 README 第0节导航 + 手册开头挂上宪法链接
- 关联：本宪法与 B 清单、手册、速查表同体系

### 2026-08-23 — 系统全貌解剖与文档化（只读阶段）
- 完整重读 App 18 个 Java + Manifest + 4 个 res/xml，与 README 100% 吻合
- 重读 H5 `index_10.js`/`index_4.js`/`element.js`/`utils.js` + 代表性页 `5楼总经理.html`
- 补读 `亚中.json` 尾部：发现**联克继电器 `8xxxxxx`** 完整转换逻辑（之前漏），已补入 README 9.3/9.5
- 确认 `ya_control-html/` 与原始 `亚中中控控制优化/` 逐文件 MD5 零差异（=部署前端优化版）
- 产出三份"地图"文档：`docs/系统解剖手册.md`、`docs/设备点号速查表.md`、`docs/工作日志.md`
- 标记 15 个已知问题（B-01~B-15），均未修复（本阶段只读）
- 发现额外系统组件：`com.jyc.keepalive`（保活 App，Manifest `<queries>` 引用，独立安装）

### （更早）— 初始文档化
- 建立 `README.md`（9 节架构总览）
- 复制 `ya_control` 进工作区 `yzyl-0823/ya_control`，复制前端为 `ya_control-html/`

---

## 三、日志模板（新增记录请复制此块，填在"二"最上方）

```
### YYYY-MM-DD — 标题
- 做了什么（读/改/排障）
- 涉及文件
- 结论 / 发现
- 关联 bug 编号（如有）
- 下一步
```

## 四、修复记录模板（修复 bug 后填，并在"一"对应行改状态）

```
### [修复] B-xx — YYYY-MM-DD
- 现象：
- 根因：
- 改法（文件+要点）：
- 验证：
- 风险：
```
