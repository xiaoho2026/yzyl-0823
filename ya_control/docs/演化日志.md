---
title: 演化日志（文档即代码）
description: 每次改代码的变更史：E-00 初始基线 + 后续 E-xx，含回退/验证
type: 按需
---

# 演化日志（Evolution Log · 文档即代码的变更史）

> **本文件是"文档即代码"的执行证据**（见 `docs/开发宪法.md` 第 5B 章）。
> 每次**改代码**（含改 Node-RED 流、前端、conf.ini、配置）都必须在此写一条演化记录，
> 让 LLM（主要）和未来的自己知道：**改了什么、为什么、怎么发生的、如何回退、验证结果**。
> 不写演化记录的代码改动 = 视为未完成、禁止上线。
>
> **规则**：
> - 按时间**倒序**（新在最上），每条带唯一编号 `E-xx`。
> - 初始基线 `E-00` 固化"读懂时系统原貌"，是所有后续演化的对照锚点。
> - 任何改动同步更新 `代码解说/`、`系统解剖手册`、`设备点号速查表`、`工作日志`，并在本条"关联文档"注明。

---

## E-00 · 初始基线（2026-08-23）
- **触发**：项目复制进工作区，开始理解原系统
- **改动文件清单**：
  - `ya_control/`（App 源码，原样复制，未改）
  - `ya_control-html/`（前端工程，与原始 `亚中中控控制优化/` 逐字节一致，MD5 校验）
  - `亚中.json`（Node-RED 流，外部参考，未改）
  - `Debug/中控上传程序.exe`（PC 工具，二进制，未改）
- **动机**：建立"读懂时"的系统快照，作为未来所有改动的对照锚点
- **具体改法**：无代码改动，仅复制 + 文档化
- **回退方式**：git 初始 commit / 原始文件备份
- **验证**：App 可 Android Studio 打开；前端 MD5 与原始一致
- **关联文档**：`README.md`(总览)、`系统解剖手册.md`、`设备点号速查表.md`、`工作日志.md`(B-01~B-15)、`代码解说/`(全部 20 文件)、`开发宪法.md`
- **系统原貌要点**（未来改动若偏离这些，先想清楚）：
  - 前端入口真相：`总控选择.html`（非 `index_android.html`，后者仅 Constant 默认值）
  - 中控逻辑在 Node-RED，不在 App
  - 硬协议边界：MQTT topic / TCP 帧 / 点号分段 / JS Bridge 方法名（见宪法 4.4）
  - 四层部署物须版本配套：App / Node-RED流 / 前端 / PC工具

---

<!-- 未来改动示例（复制此块，填在 E-00 上方）：
## E-01 · 标题（YYYY-MM-DD）
- 触发：现场 bug / 需求
- 改动文件清单：app/.../X.java, 亚中.json#节点Y, ya_control-html/js/Z.js
- 动机：为什么改
- 具体改法（要点，不给大段代码）：...
- 回退方式：git revert <commit> / 恢复 .bak / 还原 conf.ini
- 验证：真机冒烟 checklist 全过（见宪法第7章）
- 关联文档：同步更新了 代码解说/X.md、速查表、手册第3节、B-xx 标已修复
-->

---

## 演化纪律提醒（每次改动前默念）
1. 这是现场 bug 还是潜在隐患？→ 潜在只标 B 清单，不修（宪法 4.1）
2. 动的是不是硬协议边界？→ 是则禁止单独改（宪法 4.4）
3. 改完能单独回退吗？→ 不能则禁止（宪法 4.3）
4. 能在真机验证吗？→ 不能则转标记（宪法 4.8）
5. 改了代码，三件套文档 + 本演化日志同步了吗？→ 没同步 = 改动未完成（宪法 5B.2）
